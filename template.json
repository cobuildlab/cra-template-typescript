{
  "package": {
    "private": true,
    "version": "1.0.0",
    "scripts": {
      "eslint": "eslint --ext .js,.jsx,.ts,.tsx ./src/",
      "install:deps": "npm install typescript @testing-library/react @testing-library/user-event @testing-library/jest-dom",
      "install:devdeps": "cross-env HUSKY_SKIP_INSTALL=1 npm install --save-dev @typescript-eslint/parser @typescript-eslint/eslint-plugin eslint-config-airbnb-typescript eslint-config-prettier eslint-plugin-import eslint-plugin-jest eslint-plugin-jsdoc eslint-plugin-jsx-a11y eslint-plugin-prettier eslint-plugin-react eslint-plugin-react-hooks husky lint-staged prettier prettier-eslint",
      "install:types": "npm install @types/jest @types/node @types/react @types/react-dom",
      "cleanup": "node -e \"try{require('./postsetup')}catch(e){}\"",
      "setup": "npm i -D cross-env && npm run install:deps && npm run install:devdeps && npm run install:types && npm run cleanup",
      "postinstall": "npm run setup"
    },
    "husky": {
      "hooks": {
        "pre-commit": "lint-staged",
        "pre-push": "cross-env CI=true npm run test"
      }
    },
    "lint-staged": {
      "*.{ts,tsx,js,jsx}": [
        "prettier --write",
        "eslint --fix",
        "git add"
      ]
    }
  }
}